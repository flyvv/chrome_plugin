import './app.less';

import React, { useEffect, useRef, useState } from 'react';

export default function App() {
  return (
    <div
      className="popup"
      style={{
        width: '800px',
        height: '600px',
      }}
    >
      hello word
    </div>
  );
}
